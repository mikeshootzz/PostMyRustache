name: Build and Release

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            rust_target: x86_64-unknown-linux-gnu
            asset_name: postmyrustache-linux-x86_64.zip
          - target: x86_64-pc-windows-msvc
            rust_target: x86_64-pc-windows-msvc
            asset_name: postmyrustache-windows-x86_64.zip

    steps:
      - uses: actions/checkout@v2

      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: ${{ matrix.rust_target }}
          override: true

      - name: Build binary
        run: cargo build --target ${{ matrix.rust_target }} --release

      - name: Archive production artifacts
        run: |
          if [ "${{ matrix.target }}" = "x86_64-pc-windows-msvc" ]; then
            zip ${{ matrix.asset_name }} target/${{ matrix.rust_target }}/release/postmyrustache.exe
          else
            zip ${{ matrix.asset_name }} target/${{ matrix.rust_target }}/release/postmyrustache
          fi

      - name: Upload artifact for release
        uses: actions/upload-artifact@v2
        with:
          name: ${{ matrix.asset_name }}
          path: ${{ matrix.asset_name }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v2
        with:
          path: .

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false

      - name: Upload Release Asset Linux x86_64
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./postmyrustache-linux-x86_64.zip
          asset_name: postmyrustache-linux-x86_64.zip
          asset_content_type: application/zip

      - name: Upload Release Asset Windows x86_64
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./postmyrustache-windows-x86_64.zip
          asset_name: postmyrustache-windows-x86_64.zip
          asset_content_type: application/zip
